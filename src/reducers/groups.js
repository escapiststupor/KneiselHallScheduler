// import { CREATE_GROUP, DELETE_GROUP, SET_COACH, SET_MEMBERS, SET_REHEARSAL_PLACE } from './actions'; from UI
// import { SET_REHEARSAL_TIME, SET_COACHING_TIME } from './actions'; automatically

export default function groups(state = {}, action) {
  switch (action.type) {
    default:
      return state;
  }
}
